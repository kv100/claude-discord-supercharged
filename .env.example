# Required
DISCORD_BOT_TOKEN=your-bot-token-here

# Optional: Working directory for Claude sessions (default: process.cwd())
# CLAUDE_CWD=/path/to/working/directory

# Optional: Port for health check endpoint (default: 8080)
# PORT=8080
